# Building
```bash
# cp ../frontend-core/.env.development ../frontend-core/.env.production
yarn build-assets # you may have to remove some console.logs if it's not building properly
yarn build-from-source # tauri repo cloned at the same level as guijs needed
```
