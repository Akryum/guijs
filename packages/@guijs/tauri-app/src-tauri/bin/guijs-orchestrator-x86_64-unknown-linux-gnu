#!/usr/bin/env sh

# if tauri dies, kill guijs

# get tauri (parent PID)
tauri_pid=$1

# get node
node_pid=$2

while true
do
  # is the tauri pid offline
  if ["$(ps -p $tauri_pid)" -eq ""]; then
    finish
  fi
done

function finish {
  pkill -P $node_pid
  pkill -P $tauri_pid
}

trap finish EXIT